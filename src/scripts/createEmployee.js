//Function definition
const createEmployee = function(firstName, lastName, role, accountNumber) {
  return {
    firstName: firstName,
    lastName: lastName,
    role: role,
    accountNumber: accountNumber
  }
}
